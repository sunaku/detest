Dfect is an assertion testing library for Ruby that emphasizes a simple
assertion vocabulary, instant debuggability of failures, and flexibility in
composing tests.

%#----------------------------------------------------------------------------
### Features
%#----------------------------------------------------------------------------

* Adds only 7 mnemonic method names to your memory:

  `D`escribe, `T`rue, `F`alse, `E`rror, `C`atch, `S`hare, and `L`og.

* Lets you debug assertion failures interactively.

* Lets you nest tests, assertions, and execution hooks.

* Maintains a detailed report of assertion failures.

* Implemented in <%= `sloccount lib/dfect.rb`[/^\d+/] %> lines of code.

%#----------------------------------------------------------------------------
### Resources
%#----------------------------------------------------------------------------

* Issue tracker (report bugs, request features, get help):
  <http://github.com/sunaku/dfect/issues>

* Source code (browse online or obtain with [Git](http://git-scm.com)):
  <http://github.com/sunaku/dfect>

* API documentation:
  <<%= File.join(Dfect::WEBSITE, 'api/') %>>

* Announcements feed:
  <<%= File.join(Dfect::WEBSITE, 'ann.xml') %>>

* Official website:
  <<%= Ember::WEBSITE %>>

%#----------------------------------------------------------------------------
### Setup
%#----------------------------------------------------------------------------

Prerequisites:

  * [Ruby] 1.8.6 or newer.

  * [RubyGems] 1.3.6 or newer.

  * [ruby-debug] will be used to help you debug assertion
    failures interactively if it is installed.  Otherwise, the
    standard interactive Ruby shell (IRB) will be used instead.

Installing:

    gem install dfect

Upgrading:

    gem update dfect

Removing:

    gem uninstall dfect
